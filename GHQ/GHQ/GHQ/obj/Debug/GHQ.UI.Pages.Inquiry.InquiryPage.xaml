<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             	 xmlns:i18n="clr-namespace:Service.Localization;assembly=GHQ"
              Title="{i18n:Translate PageHeader_Inquiry}"
             x:Class="GHQ.UI.Pages.Inquiry.InquiryPage">


  <RelativeLayout>
    <Image  Opacity="0.3"  Aspect="AspectFill"
                RelativeLayout.WidthConstraint=
                  "{ConstraintExpression Type=RelativeToParent, Property=Width}"
                RelativeLayout.HeightConstraint=
                  "{ConstraintExpression Type=RelativeToParent, Property=Height}">
      <Image.Source>
        <OnPlatform x:TypeArguments="ImageSource" WinPhone="Assets/background.png" Android="background.png" iOS="background"/>
      </Image.Source>
    </Image>

    <Grid Style="{StaticResource MainGridStyle}" RelativeLayout.WidthConstraint=
              "{ConstraintExpression Type=RelativeToParent, Property=Width}"
            RelativeLayout.HeightConstraint=
              "{ConstraintExpression Type=RelativeToParent, Property=Height}">

      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <Grid Style="{StaticResource GridContentStyle}">
        <ScrollView>
          <Grid >
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListView  Grid.Row="0" ItemsSource="{Binding ValidationErrors}" IsVisible="{Binding HasErrors}" Style="{StaticResource ErrorListViewStyle}">
              <ListView.ItemTemplate>
                <DataTemplate>
                  <ViewCell>
                    <ViewCell.View>
                      <Grid Padding="10,5">
                        <Label  Text="{Binding Error}"  Style="{StaticResource ErrorLabelStyle}" />
                      </Grid>
                    </ViewCell.View>
                  </ViewCell>
                </DataTemplate>
              </ListView.ItemTemplate>
            </ListView>

            <StackLayout Grid.Row="1" Style="{StaticResource FormStackLayoutStyle}" Orientation="Vertical">

              <Entry Placeholder="{i18n:Translate Inquiry_Entry_PlaceHolder_MobileNumber}"  Text="{Binding LoginData.Mobile}"  Style="{StaticResource EntryStyle}" />

              <Entry Placeholder="{i18n:Translate Inquiry_Entry_PlaceHolder_FirstName}"   Text="{Binding LoginData.Password}"  Style="{StaticResource EntryStyle}" />

              <Entry Placeholder="{i18n:Translate Inquiry_Entry_PlaceHolder_FullName}"   Text="{Binding LoginData.Mobile}"  Style="{StaticResource EntryStyle}" />

              <Entry Placeholder="{i18n:Translate Inquiry_Entry_PlaceHolder_Email}"   Text="{Binding LoginData.Password}"  Style="{StaticResource EntryStyle}" />

              <Entry Placeholder="{i18n:Translate Inquiry_Entry_PlaceHolder_Address}"   Text="{Binding LoginData.Mobile}"  Style="{StaticResource EntryStyle}" />

              <Entry  Placeholder="{i18n:Translate Inquiry_Entry_PlaceHolder_Details}"  Text="{Binding LoginData.Password}"  Style="{StaticResource MultiLineEntryStyle}" />

            </StackLayout>

          </Grid>

        </ScrollView>
      </Grid>

      <ActivityIndicator Grid.Row="1" IsRunning="{Binding IsLoading}" Style="{StaticResource ActivityIndicatorStyle}"/>

      <Button Grid.Row="2" Style="{StaticResource ButtonSolidStyle}" Text="{i18n:TranslateExtension Text=Inquiry_Btn_Send}"   Command="{Binding OnLoginCommand}"></Button>

    </Grid>
  </RelativeLayout>

</ContentPage>