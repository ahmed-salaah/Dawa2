<?xml version="1.0" encoding="UTF-8"?>
<Base:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:Base="clr-namespace:GHQ.UI.Pages;assembly=GHQ"
               xmlns:controls="clr-namespace:GHQ.UI.Pages.PhotoGallery;assembly=GHQ"
               xmlns:i18n="clr-namespace:Service.Localization;assembly=GHQ"
               Title="{i18n:Translate Home_Navigation_MediaCenter}"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GHQ.UI.Pages.PhotoGallery.AlbumLibraryPage">
  <RelativeLayout>
    <Image  Opacity="0.3" Aspect="AspectFill"
                RelativeLayout.WidthConstraint=
                  "{ConstraintExpression Type=RelativeToParent, Property=Width}"
                RelativeLayout.HeightConstraint=
                  "{ConstraintExpression Type=RelativeToParent, Property=Height}">
      <Image.Source>
        <OnPlatform x:TypeArguments="ImageSource" WinPhone="Assets/background.png" Android="background.png" iOS="background"/>
      </Image.Source>
    </Image>

    <Grid  Style="{StaticResource MainGridStyle}" RelativeLayout.WidthConstraint=
              "{ConstraintExpression Type=RelativeToParent, Property=Width}"
            RelativeLayout.HeightConstraint=
              "{ConstraintExpression Type=RelativeToParent, Property=Height}">

      <Grid  Grid.Row="0" Style="{StaticResource GridContentStyle}">

        <Grid.RowDefinitions>
          <RowDefinition Height="15" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ActivityIndicator Grid.Row="0" IsRunning="{Binding IsLoading}" Style="{StaticResource ActivityIndicatorStyle}"/>
        <ListView ItemsSource="{Binding Albums}" SelectedItem="{Binding SelectedAlbum}"  ItemTapped = "Handle_ItemTapped" ItemTemplate="{StaticResource AlbumLibraryTemplate}"  RowHeight="110"  Grid.Row="1" BackgroundColor="#fef5ec"/>

      </Grid>
    </Grid>
  </RelativeLayout>
</Base:BasePage>